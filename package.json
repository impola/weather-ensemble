{
  "name": "weather-ensemble",
  "version": "1.0.0",
  "description": "Multi-model ensemble weather forecasting app",
  "scripts": {
    "cap:sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/geolocation": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
